import System.Collection;

Type ti = typeof(int);
Type ts = typeof(Array);

int res0 = ti.getInterfaces().length;
int res2 = ts.getInterfaces().length; // Array : System.Util.IIndexable, System.Util.IIterable
